services:
  api:
    image: insurance-core-api:latest
    container_name: insurance-core-api
    env_file:
      - ../.env
    environment:
      APP_NAME: insurance-core-api
      APP_ENV: development
      HTTP_PORT: 8080
    ports:
      - "8080:8080"
    restart: unless-stopped
